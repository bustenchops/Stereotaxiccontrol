class testing:

    def __init__(self, numA, numB):
        self.varA = numA
        self.varB = numB

    def additnow(self,numC,numD):
        result = (self.varA * self.varB) + (numC + numD)
        return result
    def resulttt(self):
        return result